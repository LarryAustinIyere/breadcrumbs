import React from 'react';
import { Button } from '@mui/material';

const Home = () => {
    return (
        <div>
            <Button href='/myname'>MY NAME</Button>
        </div>
    );
};

export default Home;
