import React from 'react'
import { Button } from '@mui/material';

const Root = () => {
    return (
        <div>
            <Button href='/home'>HOME</Button>
        </div>
    );
};

export default Root;
